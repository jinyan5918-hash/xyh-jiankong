# 用法（推荐 Cookie 很长时用）：
# 1) 复制本文件为 /etc/jiankong/douyin.env （路径可自定，与下面一致即可）
#    sudo mkdir -p /etc/jiankong
#    sudo cp douyin.env.example /etc/jiankong/douyin.env
#    sudo chmod 600 /etc/jiankong/douyin.env
#    sudo nano /etc/jiankong/douyin.env   # 填真实值
#
# 2) 在 jiankong-api.service.d/override.conf 的 [Service] 里增加一行（仅此一行即可承载长 Cookie）：
#    EnvironmentFile=-/etc/jiankong/douyin.env
#    （减号表示文件不存在不报错；去掉减号则文件必须存在）
#
# 3) sudo systemctl daemon-reload && sudo systemctl restart jiankong-api
#
# 说明：每行 KEY=VALUE，VALUE 一般不要加引号；若值内必须含换行请用文档外方式处理。

DOUYIN_USE_PLAYWRIGHT=1
# DOUYIN_COOKIE=
# DOUYIN_PROXY_POOL=
# DOUYIN_PLAYWRIGHT_PROXY=

# --- 抖音开放平台「查询特定视频的视频数据」（仅适用：授权用户本人上传的公开视频）---
# 详见仓库 docs/DOUYIN_OPENAPI_INTEGRATION.md
# DOUYIN_USE_OPENAPI=1
# DOUYIN_OPENAPI_USER_ACCESS_TOKEN=act.xxxxxxxxx
# DOUYIN_OPENAPI_OPEN_ID=ba253642-0590-40bc-9bdf-9a1334b94059
# DOUYIN_OPENAPI_ITEM_MAP_JSON={"https://v.douyin.com/你的短链/":"@你的open_item_id"}

# --- 小程序 ticket 换用户 access_token（POST /douyin/open-auth/ticket）---
# 在开放平台控制台取小程序的 client_key（常同 AppID）与 App Secret
# DOUYIN_OPEN_PLATFORM_CLIENT_KEY=tt你的AppID
# DOUYIN_OPEN_PLATFORM_CLIENT_SECRET=你的AppSecret
# 可选：要求小程序请求带 Authorization: Bearer <该值>，防接口被扫
# DOUYIN_OPENAUTH_CALLBACK_SECRET=随机长字符串
