# 用法（推荐 Cookie 很长时用）：
# 1) 复制本文件为 /etc/jiankong/douyin.env （路径可自定，与下面一致即可）
#    sudo mkdir -p /etc/jiankong
#    sudo cp douyin.env.example /etc/jiankong/douyin.env
#    sudo chmod 600 /etc/jiankong/douyin.env
#    sudo nano /etc/jiankong/douyin.env   # 填真实值
#
# 2) 在 jiankong-api.service.d/override.conf 的 [Service] 里增加一行（仅此一行即可承载长 Cookie）：
#    EnvironmentFile=-/etc/jiankong/douyin.env
#    （减号表示文件不存在不报错；去掉减号则文件必须存在）
#
# 3) sudo systemctl daemon-reload && sudo systemctl restart jiankong-api
#
# 说明：每行 KEY=VALUE，VALUE 一般不要加引号；若值内必须含换行请用文档外方式处理。

DOUYIN_USE_PLAYWRIGHT=1
# DOUYIN_COOKIE=
# DOUYIN_PROXY_POOL=
# DOUYIN_PLAYWRIGHT_PROXY=
