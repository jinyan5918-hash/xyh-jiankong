================================================================================
                        企业版抖音点赞监控 — Windows 客户端
================================================================================

更新日期：2026-03-30
版本号：1.2.11
（说明：版本号须与下列文件保持一致——client/release_version.txt、程序窗口标题 v…、打包出的 exe 名称 EnterpriseDouyinClient-版本号）

--------------------------------------------------------------------------------
【本次更新内容】（v1.2.11）
--------------------------------------------------------------------------------
1. 修复左下角状态栏偶发不显示：状态栏改为优先占位，再渲染中间任务/日志区域，保证固定可见。
2. 提升作者昵称自动识别成功率：服务端接口优先使用 Playwright 解析，失败再回退 HTTP 解析。
3. 保留 v1.2.10 的「名称」输入框浅灰占位“自动识别”与自动填充逻辑。

--------------------------------------------------------------------------------
【上一版本 1.2.10】（2026-03-30）
--------------------------------------------------------------------------------
1. 恢复「名称」输入框，并使用浅灰占位提示「自动识别」；输入框失焦且为空时自动回填提示。
2. 填入视频链接后仍会自动识别作者昵称并写入名称框；若识别失败，创建/更新任务时回退为「未命名任务」，不会把占位文字当真实名称提交。
3. 其余 v1.2.9 的功能保持不变（客户端更名、去顶栏、日志 Session 修复配套）。

--------------------------------------------------------------------------------
【上一版本 1.2.9】（2026-03-30）
--------------------------------------------------------------------------------
1. 产品名改为「企业版账号辅助客户端」（窗口标题与欢迎页一致）；去掉主界面顶部装饰条以节省纵向空间。
2. 左下角恢复固定显示状态栏（与监控/调度信息同步）；任务列表与监控日志区域布局不变。
3. 新增任务时不再手填名称：填入视频链接并离开输入框后，由服务端解析作者昵称并自动作为任务名；创建前也会再拉取一次以免遗漏。
4. **须配合服务端更新**：修复调度器日志中「Object is already attached to session」类错误（任务在独立 Session 内重新加载）。

--------------------------------------------------------------------------------
【上一版本 1.2.8】（2026-03-30）
--------------------------------------------------------------------------------
1. 主界面用上下分割条固定「任务列表」与「监控日志」区域，避免窗口较小时日志区被挤到屏幕外看不到；日志区可拖动分割条调整高度。
2. 「新增任务」行仅保留名称/链接/步长与「新增任务」按钮；「刷新列表」「更新选中」「删除选中」移到「暂停/恢复/停用/启用任务」同一行，并排在「启用任务」之后。
3. 上述任务操作按钮使用略窄内边距样式，减少按钮左右留白。

--------------------------------------------------------------------------------
【上一版本 1.2.7】（2026-03-30）
--------------------------------------------------------------------------------
1. 「目标点赞」改为「每增长多少赞提醒」：创建/更新任务时可设置步长（如每增长 10/20 赞提示一次，0 表示不提醒）；提醒同时支持系统通知与企业微信群机器人推送。
2. 任务列表改为更简洁：展示「当前点赞」「评论数」「任务状态」，不再展示目标点赞；支持按 ID/当前点赞/评论数排序。
3. 窗口尺寸更紧凑，任务表支持上下/左右滚动，便于管理大量任务。
4. 新增评论监控：视频出现新评论时提示一次（若能抓到则附带最新评论摘要）；任务列表显示评论数。

--------------------------------------------------------------------------------
【上一版本 1.2.6】（2026-03-28）
--------------------------------------------------------------------------------
1. 任务列表在「目标点赞」前增加「当前点赞」列（服务端最近一次成功爬取值；尚无记录时显示「—」）。
2. 列表支持排序：按 ID、目标点赞、当前点赞（高→低 / 低→高等），下拉框切换后立即重排（无需重新请求）。
3. 支持对选中任务单独「暂停 / 恢复 / 停用 / 启用」（与顶栏「暂停监控」「停止监控」作用于您名下全部任务不同）；右键菜单含相同操作。
4. 原「启用」列改为「任务状态」中文：监控中、已暂停、已停用（不再显示英文 true/false）。**须将服务器部署到同版本 API**（含 `task_paused` 字段与列表中的 `current_likes`）。

--------------------------------------------------------------------------------
【上一版本 1.2.5】（2026-03-28）
--------------------------------------------------------------------------------
1. 登录页文案层级调整：第一行为产品名（略小、灰紫字），中间为「系统已为你开启好运模式～」（加大加粗、主色），底部为内部使用提示（更小字号、柔和玫色）；删除「界面清爽好用，祝工作顺利」。
2. 版本号改为固定在欢迎页右下角灰色小字；表单与标题区整体居中对齐，留白与间距优化。

--------------------------------------------------------------------------------
【更早版本 1.2.4】（2026-03-28）
--------------------------------------------------------------------------------
1. 登录页顶部增加 Canvas 绘制的粉嫩海浪、云朵、星星与简笔卡通小猫（无外部图片依赖）；欢迎主文案改为「系统已为你开启好运模式～」。
2. 登录后主界面顶部增加同款风格的窄条海浪与小猫装饰，与整体浅紫粉主题统一。

--------------------------------------------------------------------------------
【更早版本 1.2.3】（2026-03-30）
--------------------------------------------------------------------------------
1. 界面整体调整为柔和浅紫粉配色，按钮与表格选中态更清爽；Windows 下默认使用更易配色的界面主题。
2. 欢迎页增加友好提示文案；合规说明改为偏玫瑰色强调（减少刺眼大红）。
3. 未配置 Webhook 时的锁定提示改为「温馨提示」口吻，用语更温和。
4. 默认使用「Microsoft YaHei UI」等系统字体，行距与列表行高略增，长时间观看更舒适。

--------------------------------------------------------------------------------
【更早版本 1.2.2】（2026-03-30）
--------------------------------------------------------------------------------
1. 点赞达标提醒：不再自动将客户端主窗口置顶/抢前台；仍保留系统通知、提示音，打开窗口后对应任务在列表中高亮。

--------------------------------------------------------------------------------
【更早版本 1.2.1】（2026-03-29）
--------------------------------------------------------------------------------
1. 启动时先显示登录欢迎页，标注「星与海/创左内部使用，泄露追责」，登录后再进入功能界面。
2. Webhook 在本地 user_prefs.json 缓存一份：服务端暂未返回时自动带出上次填写内容，保存成功后同步写入本地。
3. 服务端企业微信调用增强错误处理（HTTP/网络/非 JSON 返回），便于排查「测试通知」失败原因（仍须部署新版 main/wecom）。
4. 点赞达标后：若点赞数继续上升，会再次推送企业微信与客户端提醒（低于目标后会重置，再次达标可再推）；不再「只在首次达标推一次」。
5. 管理员后台员工列表增加「删除用户」（不可删 admin），将删除该用户任务、设备、提醒等关联数据。

--------------------------------------------------------------------------------
【使用说明】
--------------------------------------------------------------------------------
1. 将整个文件夹解压到任意目录，勿删除同目录下的 config.json、release_version.txt、user_prefs.json（若有）等文件。
2. 双击「EnterpriseDouyinClient-版本号.exe」启动；在欢迎页登录。账号与密码由管理员在后台创建/分配。
3. config.json 中的 api_base 为服务器地址（与浏览器访问后台时一致，不要带 /admin）。
4. 员工填写 Webhook 后务必点「保存企业微信通知」；管理员也可在后台预填。测试企微前请确认服务器已部署最新 API 并已重启。
5. 若无法登录或收不到企微消息，请把本文件中的「版本号」「更新日期」及 build_info.txt（若有）发给管理员排查。

--------------------------------------------------------------------------------
【维护备忘】每次发新版请同步修改本文件并提交 Git
--------------------------------------------------------------------------------
- 更新「更新日期」「版本号」「本次更新内容」。
- 同步修改 client/release_version.txt 与 windows_client.py 中的 CLIENT_VERSION_FALLBACK。
- 确认 GitHub Actions 与 build_enterprise_client.bat 会把本文件复制进 dist 目录（随 zip 分发）。
